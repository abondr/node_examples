module.exports = {
    'counter': function (arr) {
        return `there are ${arr.length} number of items in the array`;
    },
    'adder': function (a, b) {
        return `summetion of ${a} and ${b} is ` + (a + b);
    },
    'valueOfPi': function () {
        return 22.0 / 7.0;
    }
}